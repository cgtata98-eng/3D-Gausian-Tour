# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 3D scene assets — too large for git, hosted on R2 instead.
# Keep scene.json, floorplan.png, audio, mediapipe wasm; exclude splat blobs.
*.ply
*.spz
*.sog
public/assets/scenes/sample/splat.*
public/assets/scenes/**/splat.*
public/assets/scenes/**/collision/*.glb

# R2 upload staging — large bin assets we hand-upload to object storage
r2-uploads/**/splat.*
r2-uploads/**/*.ply
r2-uploads/**/*.spz
r2-uploads/**/*.sog
# 360°動画の素材 (Debug の「反転素材を作る」が原本と反転をここに置く)。
# 1 本で数十 MB〜GB になるので git には載せない。
r2-uploads/**/*.mp4
r2-uploads/**/*.webm

# OS / IDE noise
Thumbs.db
ehthumbs.db
Desktop.ini

# Build / temp
.cache/
.tmp/
.temp/
build/

# Screenshots written by scripts/shot.mjs — regenerated on every run
.shots/

# Environment / secrets
.env
.env.*
!.env.example
